# AI control research session — 2026-09-18 12:38 UTC

## Provenance and sequence gate

Propulsion completed first as PR #56 and merged at `b64b0e576f04f6f5833fe5175f5fae3d25b3832f` after its exact merge tree passed 190 tests in 125.826 seconds plus the benchmark. The propulsion matrix and session were read back from that merge. This AI branch started from the same exact commit and modifies no propulsion scientific content.

The shared operating protocol, AI README, ROADMAP, charter, error log, handoff, latest AI session, experiment-001 report/code and issue #42 were read before work. No open PR overlapped this phase.

## External activity and migration check

Accessible AI issues, issue comments, open PRs and recent commits showed no substantive external correction, experiment, review, collaboration offer or safety issue. Issue #42 had no comments. Surfaces not exposed through the connector are not claimed empty.

The intended standalone repository was still at owner commit `1192862f65020ce2fa27acf3ff43c2c15aa4c568` with the owner-uploaded `image (3).png`. Connected-app branch creation `access-check-20260918-1238` returned HTTP 403 `Resource not accessible by integration`. No destination branch or file was created. The owner file remains untouched; current isolated paths remain canonical.

## ONE bounded advance

Preregistered experiment 002 at `research/ai-control/preregistrations/002-monitor-enforced-stop.md`.

The frozen comparison keeps the in-memory board and monitor present in both arms and changes exactly one arm-level variable:

- logging-only: `monitor_enforced_stop=false`;
- intervention: `monitor_enforced_stop=true`, setting a monotonic stop after the detection event is committed and before any next agent event.

It reserves fresh seeds 420000–420999, with 500 violation-present and 500 benign negative-control pairs, balanced arm order and feasibility. Maximum budget is 2,000 arm-runs and 8,000 agent events. The primary outcome is the paired risk difference in completed restricted-hint use among violation-present pairs. H1 requires a point estimate no greater than -0.90 and a 95% paired-bootstrap upper bound below zero.

The design separately freezes detection sensitivity, attempted disclosure, controlled-stop rate, post-detection event count, benign false stops, task success/safe exit, reward manipulation, Wilson intervals, a 10,000-resample paired bootstrap with seed 420042, containment, abort and falsification rules.

No code was changed and no safeguard or reserved-seed experiment ran.

## Evidence and safety status

This is a prospective design for deterministic scripted software only. It cannot establish general AI control, learned-model behavior, consciousness, motive, deception, real-world incidence or catastrophe probability.

A future implementation must distinguish expected `CONTROLLED_STOP` from containment/integrity `ABORTED`, prove detection-before-stop ordering, and prevent every post-detection agent event in the enforced arm. No network, subprocess, real target, credential, exploit, self-replication, background process or uncontrolled communication is permitted.

Original documentation is CC BY 4.0 under repository policy; no third-party code, data or asset was added.

## Actual tests

Documentation/preregistration only. No AI experiment or implementation test ran in this phase. Full repository CI and the benchmark are required on the exact PR merge tree before merge.

## Blocker

Standalone migration remains blocked by the connected app's HTTP 403.

## NEXT ONE TEST

Implement issue #42's deterministic safeguard state and nonconfirmatory unit fixtures strictly against preregistration 002. Prove detection-before-stop, zero post-detection agent events, benign no-stop behavior, explicit controlled-stop semantics, exact paired configuration and forbidden-capability absence. Do not run seeds 420000–420999.
