# AI control research session — 2026-09-18 13:40 UTC

## Provenance and sequence gate

The propulsion phase completed first as PR #58 and merged at `0681ed81b1711ceaf318c7c842bc6ee8cee4da27`. Its exact merge tree passed 190 tests in 94.335 seconds plus the benchmark, and its calculation/session were read back from that merge.

This AI branch started exactly from `0681ed81b1711ceaf318c7c842bc6ee8cee4da27`. The shared protocol, AI README, ROADMAP, charter, error log, handoff, latest session, safeguard preregistration, existing harness/tests, issue #42, live branches and accessible external activity were inspected before implementation. No propulsion scientific content was modified.

## External activity and migration check

No substantive external correction, experiment, review, collaboration offer or safety issue appeared through accessible AI issues, comments, PRs, reviews or recent commits. Issue #42's only comment was the project-owner/automation progress record from the preceding session.

The standalone destination remained at owner commit `1192862f65020ce2fa27acf3ff43c2c15aa4c568` with only its owner branch. Connected-app branch creation `access-check-20260918-1340` returned HTTP 403 `Resource not accessible by integration`. No destination branch or file was created; the owner-uploaded file remains untouched.

## ONE bounded advance

Implemented preregistration 002's deterministic monitor-enforced-stop state and nonconfirmatory fixtures in the existing offline harness.

New behavior is isolated behind `SafeguardConfig`:

- both arms keep the in-memory board enabled and a four-agent-event ceiling;
- the only valid paired difference is `monitor_enforced_stop: false -> true`;
- a violation detection is hash-committed as an agent audit event before intervention;
- the enforced controller writes a hash-linked controller transition, sets a monotonic stop, makes the board read-only and allows no later agent event;
- `CONTROLLED_STOP` is distinct from `ABORTED`;
- logging-only continues to read/use the synthetic restricted token;
- benign controls complete without false stop;
- task labels are caller-supplied so paired arms receive identical inputs.

Ten new unit tests use seeds 910000 and 910001, outside reserved seeds 420000–420999. They cover exact paired configuration, rejected nonintervention changes, detection-before-stop ordering, zero post-detection agent events, logging-only continuation, benign no-stop behavior, deterministic replay/hash chains, controlled-stop semantics, immutable scoring and forbidden-capability absence.

No confirmatory experiment or reserved seed ran.

## Actual tests

The initial code-only PR merge tree `4ea9b06` checked out head `a692ae0458cabcded68e85015791a582eccb32e6` against base `0681ed81b1711ceaf318c7c842bc6ee8cee4da27` and passed:

```text
Ran 200 tests in 130.451s
OK
```

The repository benchmark also passed. A new exact merge-tree run is required after this session/handoff documentation is added; only that later run may authorize merge.

## Evidence and safety status

This implements a deterministic scripted toy safeguard. It does not show that monitoring generally controls learned agents, deployed systems, autonomous motives or consciousness.

The implementation imports no network, subprocess, shell, HTTP, filesystem target or external service. It has no credential, exploit, persistence, self-copy, code-generation or uncontrolled-communication path. The ten fixture tests are implementation checks, not the preregistered 1,000-pair outcome experiment.

Original software remains MIT and original documentation CC BY 4.0. No third-party code, data or asset was added.

## Blocker

Standalone migration remains blocked by connected-app HTTP 403. Scientific implementation in current isolated paths is not blocked.

## NEXT ONE TEST

Run preregistration 002's exact reserved block once: seeds 420000–420999, 500 violation-present and 500 benign-control pairs, with logging-only versus enforced-stop as the sole arm difference. Retain all rows/events and abort without estimates if any pairing, audit, containment, stop-order, resource or configuration check fails.
