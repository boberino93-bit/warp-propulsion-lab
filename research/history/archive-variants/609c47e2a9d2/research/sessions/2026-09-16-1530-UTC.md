# Research session: 2026-09-16 15:30 UTC

**Version:** v0.3.8  
**One test:** derive the complete mixed-index conservative x-projection of covariant stress-energy conservation for rigid translation and independently test the v0.3.7 residual against the actual connection/source term.  
**Evidence level:** exact symbolic fixed-geometry identity plus regression tests; not an engine, experiment, peer review, or independent replication.

## Provenance
@GitHub `main` HEAD verified before work as `a89da5f74ad9b91190da2bbbb4a75f33c31b6e8d`. GitHub session `2026-09-16-1426-UTC.md` and Library v0.3.7 were checked and agreed on NEXT ONE TEST. Reconstructed baseline suite: **59 tests passed in 0.393 s**.

## Bounded derivation
For `ds^2=-dt^2+(dx-v f dt)^2+dy^2+dz^2`, `det(g)=-1`. Exact conservation in the x covector projection is

`partial_mu T^mu_x = (1/2) T^{ab} partial_x g_ab`.

An independent SymPy calculation built `g`, `g^-1`, all Christoffels, Ricci, scalar curvature and Einstein tensor, then simplified both sides for rigid `f(x-vt,y,z)`. Before division by `8 pi`, both sides independently reduce to

`(v^2/2) f_x (f_yy+f_zz)`.

Thus the covariant divergence vanishes pointwise. In T units each side is `v^2 f_x(f_yy+f_zz)/(16 pi)`.

For `f=exp(-r^2/sigma^2)` on the x-axis, the exact term is `v^2 x f^2/(2 pi sigma^4)`. The v0.3.7 deliberately truncated residual is `v^2 x f/(2 pi sigma^4)`. Their ratio is `f`, so the old residual is not the missing connection term.

## Falsification result
**The hypothesis that the v0.3.7 local residual itself is the omitted connection force is falsified.** The exact coordinate-flux divergence is balanced pointwise by the metric-gradient connection term, and both integrate to zero by parity/localization. No net reaction thrust is present in this exact projection.

## Validation
Added `src/covariant_x_momentum.py`, `theory/covariant_x_momentum.md`, and `tests/test_covariant_x_momentum.py`. Five tests check center parity, exact-vs-naive mismatch, velocity reversal, symmetric global cancellation and derivative-formula consistency. Full reconstructed suite: **64 tests passed in 0.460 s**.

## Error/correction record
No test failure occurred this round. Conceptual correction: v0.3.7 correctly labeled its residual as incomplete, but its proposed next step could have tempted a one-for-one cancellation against a connection term. The exact projection shows that is wrong because the conservative variables/projection differ. This correction is preserved explicitly rather than rewriting history.

## Negative result / limitations
No propulsion mechanism was found. The calculation uses the Einstein tensor of a prescribed geometry, not a constitutive material/field source. Vanishing covariant divergence is required by the Bianchi identity and does not establish realizability.

## NEXT ONE TEST
Derive the exact Eulerian energy projection `n_mu nabla_nu T^{mu nu}=0` for the same rigid geometry, isolate the local energy flux/work terms, and integrate them for a localized Gaussian. Quantify velocity and wall-scale dependence of the required energy circulation before considering acceleration power.
