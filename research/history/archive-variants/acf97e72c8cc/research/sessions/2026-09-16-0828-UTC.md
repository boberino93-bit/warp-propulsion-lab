# Research session: 2026-09-16 08:28 UTC

**Session ID:** 2026-09-16-0828-UTC  
**Version:** v0.3.0  
**Scope:** covariant dust stress-energy and observer-explicit flux gate specified by prior session.  
**Status:** derived/tested locally; no propulsion mechanism found; GitHub unavailable.

## Starting state
Read the verified v0.2.0 session and archive, including README, `theory/finite_time_dust.md`, cumulative research/error logs, code and tests. v0.2.0 established finite-time 1D test-dust characteristics and explicitly named this covariant flux calculation as the next test. GitHub repository access was attempted first but the connector returned disabled; remote HEAD remains unknown.

## One bounded advance
For `ds^2=-dt^2+(dx-beta dt)^2`, `beta=v_s f(x-v_s t)`, `det(g)=-1`, the Eulerian unit normal is `n^mu=(1,beta)`, `n_mu=(-1,0)`. The v0.2.0 congruence is `u=n`. Pressureless test dust therefore has `T^{mu nu}=epsilon n^mu n^nu`.

Eulerian measurements give `E=T_mn n^m n^n=epsilon` and local momentum density `j_i=0`. Thus coordinate `T^{tx}=epsilon beta` is not locally measured thrust.

With lapse `alpha=1`, Eulerian acceleration `a_i=D_i ln(alpha)=0`, so `n` is geodesic. Dust conservation factors as `n^nu nabla_mu(epsilon n^mu)+epsilon n^mu nabla_mu n^nu=0`; hence it reduces to `partial_t epsilon + partial_x(epsilon beta)=0`, the prior continuity equation.

For a constant-x surface, current `J^x=epsilon beta`. Relative to constant `xi=x-v_s t`, `J^x-v_s J^t=epsilon(beta-v_s)=-v_s epsilon(1-f)`, and the previous characteristic invariant makes this constant along each characteristic. This is a moving-boundary crossing identity, not a mechanical reaction force.

**Negative result:** within this test-dust model there is no non-gravitational force on the dust and no local Eulerian dust momentum density to harvest as reaction thrust. The missing physics is the 3+1 stress-energy/source that creates or changes the prescribed geometry, or an explicit interaction transferring momentum to matter/fields. The 1+1 Einstein tensor cannot provide that physical source budget.

## Reproducibility and errors
Added `theory/covariant_dust_flux.md` and `tests/test_covariant_dust_flux.py`. The first test run failed because the test imported nonexistent `shape`/`shape_gradient` names; corrected to the repository's `top_hat_shape`. Final command `python -m unittest discover -s tests -v` returned **Ran 24 tests in 0.300s — OK**. This is local mathematical/code validation, not experimental replication or GitHub CI.

## Literature context
Checked current search results and retained primary grounding in Alcubierre (1994), DOI 10.1088/0264-9381/11/5/001, and Bobrick & Martire (2021), DOI 10.1088/1361-6382/abdf6e. A 2026 observer-robust energy-condition preprint was surfaced but was not used as primary support for this derivation. No novelty claim.

## Persistence
GitHub connector returned disabled; no remote read/write, SHA or PR exists for this session. Save versioned archive and standalone session record to ChatGPT Library without overwriting v0.2.0.

## NEXT ONE TEST
Lift the prescribed shape to the standard 3+1 Alcubierre ADM metric and independently compute/check the Eulerian source quantities from the Einstein constraints, beginning with energy density and momentum density for the exact shape convention. Numerically integrate/sign-check the energy density and its scaling with bubble speed, radius and wall thickness. This tests the missing geometry-source budget; it must not be described as a realizable engine absent a physical source model.
