# Research session: 2026-09-16 19:26 UTC

**Version:** v0.3.12
**One test:** derive the local acceleration-linear terms in the exact Eulerian energy projection and require their Gaussian volume integral to reproduce the independently established v0.3.11 total-energy derivative.
**Evidence level:** analytic prescribed-geometry source bookkeeping plus regression tests; not an engine, constitutive source, experiment, peer review or independent replication.

## Provenance
@GitHub `main` HEAD independently verified at start as `ac6e4f0f5692ac6e118edf27ed9da12a4228790f`; it contains the documentation backfill but not open research PRs #9/#10. PR #10 v0.3.11 was verified open/mergeable at head `3582fb2a390fe73c11af7c9cefd980a7446cc70a`. Latest Library archive/session were v0.3.11 and listed before work. The v0.3.11 Library archive was materialized; no main/branch equivalence was assumed.

## Bounded derivation
Use the established Eulerian identity `(partial_t-L_beta)E + D_i j^i = K E + K_ij S^ij`, with `E=-v^2 q/(32 pi)`, `q=f_y^2+f_z^2`. Isolating explicit terms linear in `a=dv/dt`, `L_beta E`, `D_i j^i`, and `K E` contain no explicit `a`, while `(partial_t E)_a=-v a q/(16 pi)`.

From established ADM tensors, `K_xy=-v f_y/2`, `K_xz=-v f_z/2`, `S_xy|a=a f_y/(16 pi)`, and `S_xz|a=a f_z/(16 pi)`. Counting symmetric off-diagonal contractions twice gives `(K_ij S^ij)_a=2K_xyS_xy+2K_xzS_xz=-v a q/(16 pi)`. The acceleration-linear projection closes pointwise.

For the Gaussian, `Integral q d^3x=pi^(3/2) sigma/sqrt(2)`, hence the integrated local term is `-v a sqrt(pi) sigma/(16 sqrt(2))`, exactly v0.3.11's independent chain-rule derivative.

## Dimensions / boundaries / source
Local terms scale as `v*a*q ~ L^-3`; volume integration is dimensionless, matching `d(L)/dt` in G=c=1. Assumes fixed positive sigma, smooth Gaussian decay at infinity, and the prescribed metric/source convention. No finite apparatus boundary, constitutive source, reaction reservoir, controller or conversion efficiency is supplied.

## Falsifiability / validation
Added source, theory and five tests: pointwise closure, nonzero equal channels, velocity/acceleration reversal, exact integrated agreement with v0.3.11, and Gaussian scale/invalid-scale checks. Full reconstructed Library suite actual output: **84 tests ran in 0.696 s — OK**.

## Negative result / persistence note
No propulsion mechanism or external reaction channel was found. The local acceleration energy change is exactly accounted for by the acceleration shear-stress work term of the effective Einstein source. The branch's cumulative GitHub research log was found to lag the Library archive, so it was deliberately NOT overwritten this run; reconciliation is required rather than replacing divergent history.

## NEXT ONE TEST
Convert the Gaussian acceleration-energy requirement to an SI order-of-magnitude scale using the exact geometric-to-SI factor and audit the meaning of coordinate `v` and `a`. Quantify conservative subluminal benchmark source-energy/power scales versus laboratory-accessible power, without treating that comparison as a source construction. Use the result to decide whether this Gaussian Alcubierre branch remains prototype-relevant or should be deprioritized for experimentally accessible propulsion candidates.
