# Research session — 2026-09-17 17:26 UTC — v0.3.31

## External-activity check
Before research, public repository metadata showed main `85abe8b47608c3efcf1d0080df9dc0a402b56c2a`, zero stars, zero forks, zero open issues, and discussions disabled. The main merge was authored by project owner `boberino93-bit` at 2026-09-17T16:29:59Z and had zero commit comments. No external experimental result, correction, student inquiry, collaboration offer, or safety issue was found on the accessible surfaces. Requested social/review surfaces not independently enumerable are not claimed absent.

## Reconciliation
The session branch was created directly from verified main `85abe8b47608c3efcf1d0080df9dc0a402b56c2a`. No divergent overwrite or force push was used. The existing v0.3.30 code defines a fixed synthetic stationarity scan and an uncertainty-aware Wilson gate.

## ONE bounded advance
Attempted the preregistered 10,000-holdout-trial execution for sigma, reversal-correlated bias, thermal span, sham mismatch, and combined shift. The reference implementation recalibrates a 4,000-trial empirical threshold for every grid point and simulates long-memory noise for every null and signal trial. A faithful local reproduction exceeded the available execution window before the full preregistered grid completed. Because a partial/adaptively truncated grid would violate the stated protocol, no boundary values are reported or frozen.

This is a useful falsification/protocol result: the current implementation is computationally inefficient for the declared high-sample scan and must be refactored to freeze/reuse the identical baseline calibration threshold before the high-sample grid is executed. That refactor must be regression-checked against the existing deterministic implementation before scientific results are accepted.

## Actual tests/output
The 4,000-trial baseline calibration threshold was independently reproduced locally as `1.5585078155794357e-05 N` (15.585078155794357 µN), matching the established synthetic baseline. The complete 10,000-trial multi-grid run did not finish in the execution window; therefore no new FPR, Wilson bound, or stationarity bracket is claimed.

## Evidence status
Synthetic protocol computation only. No physical build, calibrated hardware, measured thrust, independent replication, peer review, or propulsion discovery.

## NEXT ONE TEST
Refactor the high-sample scanner to compute the frozen baseline empirical threshold exactly once and reuse it across all preregistered holdout grid points; regression-test equivalence to the existing deterministic evaluator, then execute the unchanged 10,000-trial grids.